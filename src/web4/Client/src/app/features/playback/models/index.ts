export interface LoadingModel {
    loading: boolean;
    message: string;
}

export interface RecordingQuery {
    directory: string;
}

export interface RecordingDto {
    id: number;
    file: string;
}

